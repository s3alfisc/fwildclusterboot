# progress_bar <- function(i, n){
#   width <- options()$width
#   cat(paste0(rep('.', i / n * width), collapse = ''))
#   #Sys.sleep(.05)
#   if (ii == n) cat('.')
#   else cat('\014')
# }



# n <- 26
#  for (ii in 1:n) {
#    progress_bar(ii, n = n)
#  }
